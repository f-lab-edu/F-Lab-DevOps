# DB 스토리지
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  storageClassName: standard      # minikube 기본값
  accessModes:
    - ReadWriteOnce       # 단일 노드에서 읽기/쓰기
  resources:
    requests:
      storage: 1Gi
