{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-api
  labels:
    app: {{ .Release.Name }}-api
spec:
  podSelector:
    matchLabels:
      app: {{ .Release.Name }}-api
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Traefik ingress controller만 사용자 요청을 API Pod에 전달할 수 있다.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.traefik.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.traefik.podSelector | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.api.port }}
    # Prometheus만 API 메트릭 endpoint를 수집할 수 있다.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.prometheus.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.prometheus.podSelector | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.api.port }}
  egress:
    # RDS·Redis hostname을 해석하기 위한 CoreDNS 통신이다.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.dns.namespace | quote }}
          podSelector:
            matchLabels:
              {{- toYaml .Values.networkPolicy.dns.podSelector | nindent 14 }}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # 클러스터 밖 데이터 저장소에는 환경별로 지정한 CIDR과 포트만 허용한다.
    {{- range .Values.networkPolicy.externalEgress.cidrs }}
    - to:
        - ipBlock:
            cidr: {{ . | quote }}
      ports:
        - protocol: TCP
          port: {{ $.Values.networkPolicy.externalEgress.postgresPort }}
        - protocol: TCP
          port: {{ $.Values.networkPolicy.externalEgress.redisPort }}
    {{- end }}
{{- end }}
