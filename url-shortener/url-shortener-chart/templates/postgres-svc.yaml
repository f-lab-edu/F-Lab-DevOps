# DB 내부 연결
apiVersion: v1

kind: Service
metadata:
  name: {{ .Release.Name }}-postgres-svc
spec:
  selector:
    app: {{ .Release.Name }}-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP